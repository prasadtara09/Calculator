pipeline {
  agent any
  stages {
    stage('Code') {
      steps {
        sh 'echo "code will be fetched"'
      }
    }

    stage('Build') {
      steps {
        sh 'echo "build application"'
      }
    }

  }
}